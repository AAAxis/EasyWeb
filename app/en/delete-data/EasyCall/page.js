// The address the Google Play listing links to, in the /<lang>/delete-data/<app>
// shape the store listings use. The page itself lives at /delete-account; this
// serves the same one, so the two can never say different things.
export { default, metadata } from "../../../delete-account/page";
